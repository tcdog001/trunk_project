#!/bin/bash

if [ -n "${__UTILS_NUMBER_IN__}" ]; then
	return
else
	__UTILS_NUMBER_IN__="$0"
fi

#
#$1:number
#
number_check() {
	local number="$1"
	local err=1

	if [[ -n "${number}" ]]; then
		expr ${number} + 1 &> /dev/null; err=$?
	fi

	return ${err}
}
